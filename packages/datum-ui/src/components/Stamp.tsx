// ponytail: stub — Phase 2 build per Datum §12

export interface StampProps {
  code: 'U' | 'U2' | 'IBR' | 'ISO-9001' | 'ISO-14001' | 'ISO-45001'
  href?: string
}

export function Stamp(_props: StampProps): React.ReactElement {
  throw new Error('Stamp: Phase 2 not yet built')
}
