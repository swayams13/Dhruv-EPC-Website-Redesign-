// ponytail: stub — Phase 2 build per Datum §16

export interface CardProps {
  variant: 'product' | 'project' | 'certification'
  children: React.ReactNode
}

export function Card(_props: CardProps): React.ReactElement {
  throw new Error('Card: Phase 2 not yet built')
}
