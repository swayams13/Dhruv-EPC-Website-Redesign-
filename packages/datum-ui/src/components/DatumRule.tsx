// ponytail: stub — Phase 2 build; the horizontal rule + tick signature mark

export interface DatumRuleProps {
  animate?: boolean
}

export function DatumRule(_props: DatumRuleProps): React.ReactElement {
  throw new Error('DatumRule: Phase 2 not yet built')
}
