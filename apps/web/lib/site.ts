// Canonical host — the single source every JSON-LD builder, sitemap, robots
// entry, and page's alternates.canonical must agree with (docs/mistakes.md,
// 2026-07-11: a per-file BASE redeclaration is exactly how this drifted).
export const BASE = 'https://vedantagroup.net'
