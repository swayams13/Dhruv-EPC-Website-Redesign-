# apps/web/public/exploded/pressure-vessel/

Place the Dhruv EPC home-hero exploded-view frame sequence here, named `frame-01.avif` / `frame-01.webp` through `frame-05.avif` / `frame-05.webp` (assembled → fully exploded), matching the paths referenced in `apps/web/lib/content/dhruv-epc.ts`'s `dhruvExplodedFrames`.

See `docs/exploded-view-image-generation-guide.md` for the exact generation prompts, consistency technique, and export specs (16:9, AVIF + WebP, 1600px desktop / 720px mobile). See `docs/decisions.md` (2026-07-16) for why this folder exists at all — it's a logged, scoped exception to the site's normal "real photography only" rule.

This README is a placeholder marker for an empty directory (git doesn't track empty folders) — delete it once real frames are in place, or leave it; either is fine.
