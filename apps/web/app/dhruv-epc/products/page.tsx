// Dhruv EPC product category index — VG-012 R3. Template in
// lib/product-category-pages.tsx (shared with precise-engineers).
import { productCategoryIndexPage } from '../../../lib/product-category-pages'

const impl = productCategoryIndexPage('dhruv-epc')

export const metadata = impl.metadata
export default impl.Page
