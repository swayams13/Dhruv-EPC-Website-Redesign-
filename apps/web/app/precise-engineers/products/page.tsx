// Precise Engineers product category index — VG-012 R3. Template in
// lib/product-category-pages.tsx (shared with dhruv-epc).
import { productCategoryIndexPage } from '../../../lib/product-category-pages'

const impl = productCategoryIndexPage('precise-engineers')

export const metadata = impl.metadata
export default impl.Page
